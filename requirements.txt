fastapi[standard]
sqlalchemy>=2.0
asyncpg
greenlet
uvicorn[standard]
python-dotenv>=1.0.0
# IANA time zones for zoneinfo on minimal images (e.g. python:slim) without full system tzdata
tzdata>=2024.1
